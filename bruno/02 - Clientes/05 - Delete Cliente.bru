meta {
  name: 05 - Eliminar Cliente
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/clients/{{clienteId}}
  body: none
  auth: none
}

headers {
  Accept: application/json
  Authorization: Bearer {{accessToken}}
}

assert {
  res.status: eq 204
}

tests {
  test("status=204 (No Content): cliente fue eliminado", function () {
    expect(res.getStatus()).to.equal(204);
  });
}
