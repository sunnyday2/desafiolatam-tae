meta {
  name: 06 - Verificar al Cliente Eliminado
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/clients/{{clienteId}}
  body: none
  auth: none
}

headers {
  Accept: application/json
  Authorization: Bearer {{accessToken}}
}

assert {
  res.status: eq 404
}

tests {
  test("status=404: validar que cliente no fue encontrado", function () {
    expect(res.getStatus()).to.equal(404);
  });
}
